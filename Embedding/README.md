# Embedding

Company and portfolio embedding API service.

## Run (FastAPI)

```bash
uvicorn web.api.app:app --host 0.0.0.0 --port 8002
```

## Endpoints

- `POST /embeddings/company`
- `POST /embeddings/portfolio`
